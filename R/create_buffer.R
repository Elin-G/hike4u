#' Create Buffer Value
#'
#' `create_buffer` Creates a number value with your defined buffer size.
#'
#' @param buffer the defined buffer around the POI in meters.
#' @return Returns a number value "buffer" with the size in meters.
#' @examples
#' \dontrun{
#' library(hike4u)
#'
#' # Assign 10000 meters (10 km) to buffer
#' buffer <- create_buffer(10000)
#' }
#'
#' @export

create_buffer <- function(buffer) {
  # Create a data frame with the coordinates
  buffer <- buffer

  return(buffer)
}
